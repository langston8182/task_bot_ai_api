{
  "name": "task_bot_ai_api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "bestzip build.zip * --ignore=node_modules/aws-sdk/* --ignore=.git",
    "deploy": "npm run build && echo 'Deploying zip file...'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "mongoose": "^8.12.1"
  },
  "devDependencies": {
    "bestzip": "^2.2.1"
  }
}
